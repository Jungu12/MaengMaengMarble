export type UserType = {
  userId: string;
  nickname: string;
  characterId: number;
};

export type DetailUserType = {
  id: string;
  nickname: string;
  characterId: number;
  win: number;
  lose: number;
  point: number;
};
